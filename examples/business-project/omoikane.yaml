apiVersion: omoikane.io/v1
kind: RuntimeDeployment
project: example-business
resources:
  agents:
    assistant: agents/assistant/AGENT.md
  skills:
    example: skills/example/SKILL.md
  mcpServers: {}
  schemas:
    result: schemas/result.json
